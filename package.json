{
  "name": "with-nextjs-prisma",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "yarn migrate:deploy && next start --port ${PORT-3000}",
    "migrate:dev": "prisma migrate dev",
    "migrate:deploy": "prisma migrate deploy",
    "migrate:status": "prisma migrate status",
    "generate": "prisma generate"
  },
  "dependencies": {
    "@prisma/client": "5.5.2",
    "next": "14.0.2",
    "react": "18.2.0",
    "react-dom": "18.2.0",
    "swr": "^2.2.4"
  },
  "devDependencies": {
    "@types/node": "^20.9.0",
    "@types/react": "^18.2.37",
    "prisma": "5.5.2",
    "typescript": "^5.2.2"
  }
}
